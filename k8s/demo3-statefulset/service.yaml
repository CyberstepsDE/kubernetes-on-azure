apiVersion: v1
kind: Service
metadata:
  name: demo3-service
  namespace: demo3-statefulset
  labels:
    app: demo3
spec:
  type: LoadBalancer
  ports:
    - port: 80
      name: web
      targetPort: 8080
  selector:
    app: demo3
